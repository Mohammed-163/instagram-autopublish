# ============================================================
# Autonomous AI System — Unified Requirements
# ============================================================
# Covers all phases (5-10). Minimum versions derived from
# the strictest constraint across all phases.

# ---- Core infrastructure ----
SQLAlchemy>=2.0.30,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9           # Phase7 uses psycopg2; Phase5 uses psycopg[binary] (both valid)
psycopg[binary]>=3.1.18        # Phase5/6 Supabase Postgres driver

# ---- Phase 5/6 specific (content platform automation) ----
google-generativeai>=0.8.0
gspread>=6.0.0
google-auth>=2.0.0
google-api-python-client>=2.0.0
Pillow>=10.0.0
arabic-reshaper>=3.0.0
python-bidi>=0.6.0
requests>=2.31.0
python-dotenv>=1.0.0
pytz>=2024.1
PyNaCl>=1.5.0

# ---- Testing ----
pytest>=8.0,<9.0
pytest-cov>=5.0

# ---- Operational Layer ----
apscheduler>=3.10,<4.0
PyNaCl>=1.5.0          # GitHub secret encryption (token_refresh)
