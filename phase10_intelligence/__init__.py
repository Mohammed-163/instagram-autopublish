"""
Phase 10 — Intelligence Core.

Uses relative imports throughout (from ..config.settings import ...).
The `phase10_intelligence_core` sub-package is the canonical package.

Entry point: phase10_intelligence.phase10_intelligence_core.__main__
"""
